{% extends 'base.html.twig' %}

{% block body %}
    <h1>Gallerieproduit</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gallerieProduit.id }}</td>
            </tr>
            <tr>
                <th>Imagename</th>
                <td>{{ gallerieProduit.imageName }}</td>
            </tr>
            <tr>
                <th>Updatedat</th>
                <td>{% if gallerieProduit.updatedAt %}{{ gallerieProduit.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('gallerieproduit_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('gallerieproduit_edit', { 'id': gallerieProduit.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
